# This is to practice with variables and data types

kilos_run = input("How many kilomileters did you run today? ")
miles = float(kilos_run) / 1.60934
miles = round(miles, 2)
print(f"{kilos_run} kilometers is equal to {miles} miles!")